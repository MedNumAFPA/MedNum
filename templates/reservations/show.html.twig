{% extends 'base.html.twig' %}

{% block title %}Reservations{% endblock %}

{% block body %}
    <h1>Reservations</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reservation.id }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ reservation.firstname }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ reservation.lastname }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ reservation.email }}</td>
            </tr>
            <tr>
                <th>Numéro de téléphone</th>
                <td>{{ reservation.phoneNumber }}</td>
            </tr>
            <tr>
                <th>Nom de la formation</th>
                <td>{{ reservation.formationName }}</td>
            </tr>
            <tr>
                <th>Veux être rappelé de l'atelier</th>
                <td>{{ reservation.isReminderActive ? 'Oui' : 'Non' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_reservations_index') }}">Revenir a la liste</a>

    <a href="{{ path('app_reservations_edit', {'id': reservation.id}) }}">Éditer</a>

    {{ include('reservations/_delete_form.html.twig') }}
{% endblock %}
