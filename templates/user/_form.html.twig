{# {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }} #}


{{ form_start(form) }}


{{ form_label(form.email, 'Email', {'label_attr': {'class': 'block font-medium text-gray-700'}}) }}
{{ form_widget(form.email, {'attr': {'class': 'w-full  shadow-sm focus:border-blue-500 '}}) }}

{{ form_label(form.password, 'Mot de passe', {'label_attr': {'class': 'text-gray-700'}}) }}
{{ form_widget(form.password, {'attr': {'class': 'w-full  shadow-sm focus:border-blue-500 '}}) }}


<twig:Button color="primary">
	{{ button_label|default('Ajouter') }}
</twig:Button>
<a class="mt-5" href="{{ path('app_user_index') }}">Retour</a>

{{ form_end(form) }}
